---
- name: Patch RHEL Servers
  hosts: all
  gather_facts: false

  tasks:

    - name: Fetch
      ansible.builtin.fetch:
        src: /home/ansible.cfg
        dest: /tmp/ansible.cfg
        flat: true
      delegate_to: ansibledev.shadowman.dev
